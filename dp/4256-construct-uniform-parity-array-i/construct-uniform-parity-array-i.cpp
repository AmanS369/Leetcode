class Solution {
public:
    bool uniformArray(vector<int>& nums1) {
        // int n = nums1.size();

        // int oddC = 0;
        // int evenC = 0;

        // for (int x : nums1) {
        //     if (x % 2 == 0)
        //         evenC++;
        //     else
        //         oddC++;
        // }

        // if (evenC == n  || oddC == n  || evenC == oddC)
        //     return true;

        // if (evenC > oddC && oddC > 1)
        //     return true;

        // if (evenC < oddC && evenC > 1)
        //     return true;

        return true;
    }
};